import React from 'react';
import './LoadingOverflow.scss';

/**
 * Loading overflow with loading loader indicator.
 */
const LoadingOverflow = () => (
    <div className="loading">
        <div className="loader"></div>
    </div>
);

export default LoadingOverflow;
